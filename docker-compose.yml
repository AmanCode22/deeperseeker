services:
  deeperseeker:
    build: .
    container_name: deeperseeker
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - deeperseeker_data:/app/data
    restart: unless-stopped

volumes:
  deeperseeker_data:
